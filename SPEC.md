# SPEC

## §G GOAL
static infixr.com rebuild. html+css. minimal js. mobile-OK. GH Pages host.

## §C CONSTRAINTS
- static HTML + CSS only. ⊥ framework (Tailwind, Bootstrap, etc).
- JS budget ≤ 5 KB total across all `src/js/*.js`.
- new JS ! Ari approval first. ⊥ silent add.
- modern CSS OK: `@layer`, `color-mix(in oklab)`, `clamp()`, container queries, `:user-invalid`, `100dvh`, `backdrop-filter`, `scroll-snap`, `animation-timeline: view()`, `aspect-ratio`, `@supports`, `@property`, `@starting-style` + `transition-behavior: allow-discrete`, `:has()`, `field-sizing: content`, `text-wrap: balance|pretty`. newer feats (`@property`/`@starting-style`/`field-sizing`) progressive-enhance via `@supports` where engine coverage is partial (V40–V44).
- CSS ! native nesting (nest child rules in parent w/ `&`). ⊥ flat repeated selector chains. ⊥ preprocessor (Sass/Less) — browser-native only.
- mobile-first. ! hold at 360 px width.
- progressive enhance. unsupported features → graceful fallback, never blank/broken.
- Bun toolchain. ⊥ npm/yarn.
- layout: `src/` = hand-edited source of truth; `build.ts` emits `public/` (generated, gitignored).
- GH Pages served from `public/` via GitHub Actions (`.github/workflows/deploy.yml`). `.nojekyll` emitted into `public/`.
- build pipeline = Effect.ts (Schema + Option, ⊥ null) as build-time-only LIBRARY (ships 0 client bytes — ✓ ⊥-framework). templating = typed `Data → string` render fns, ⊥ templating DSL. markdown via `marked`, build-time only, 0 client bytes.

## §I INTERFACES
- pages: each is a folder `src/pages/<page>/` with co-located `body.ts` (the `<main>` render fn) + `meta.ts` (`PageMeta` config). `index/` → home (hero, who-we-are carousel, solutions, blog teaser, contact form); `about/` → company prose + values; `blog/` → post list (data-driven from `content/posts/*.md`). build wraps each body in shared chrome → `public/{index,about,blog}.html`. (No static `src/*.html` files — bodies are typed render fns; cites V33/V35.)
- content data: `src/content.ts` → `loadPosts` (read+validate+render `content/posts/*.md` → `BlogPost[]`) + `displayDate`. The post data layer, shared by `pages/index/body.ts` (homepage teaser), `pages/blog/body.ts` (index + per-post pages), `components/glass-card` (`<time>` meta). Build-time only, 0 client bytes.
- asset: `src/assets/hero-headset-1200.{webp,png}` → hero LCP still (model-viewer frame 0); `src/assets/headset.glb` → the lazy-loaded 3D model (T27). (hero2* DELETED T40 — flat bg since V27, ∅ photo.)
- asset: `src/assets/who-{1,2,3}.{jpg,webp}` → carousel cards
- asset: `src/assets/sol-corporate.{png,webp}` → HOME blog feature card photo (NOT solutions); `src/assets/sol-industrial-feature.{png,webp}` → §our-solutions large photo card. (sol-realestate*, bare sol-industrial* DELETED T40 — non-feature, unused since the V29 grid.)
- asset: `src/assets/favicon.svg`, `favicon-32.png`, `apple-touch-icon.png`, `og-image.png`
- file: `src/manifest.webmanifest` → PWA manifest
- script: `src/js/main.js` → contact form submit handler + year stamp + who-carousel (transform-track loop, autoplay, nav). ENDPOINT placeholder `https://example.com/api/contact`. each behavior no-ops where its target is absent.
- build: `build.ts` → Effect SSG: `src/` + `content/` → `public/`. per page: decode PageMeta (Schema) → assemble shell (head+nav+body+footer from templates) → inline CSS + @font-face → inject preloads → write. emits `public/blog/<slug>.html` per post. copy assets/js/manifest, emit `.nojekyll`. NEVER writes `src/`. `bun run build.ts` (no args — hook depends).
- build: `lint.ts` → dead-token check `src/css/tokens.css` vs consumers. `bun run lint`.
- script: `server.ts` → Bun dev server, serves `public/`. `bun run server.ts` (or `bun run dev` = build+serve) → 127.0.0.1:8765
- ci: `.github/workflows/deploy.yml` → on push to `main`: build `public/` → deploy via `actions/deploy-pages`.
- vendor: `vendor/rotating-metaquest3/` → 3D MetaQuest3 source (subtree). SHIPPED via T27: `headset.glb` → `src/assets/headset.glb`; model-viewer 4.2.0 dist self-hosted → `src/js/hero-3d/model-viewer.min.js` (cross-origin unpkg in the vendored `index.html` NOT used). lazy-loaded by `src/js/hero-3d/loader.js`.
- env: none required at runtime
- remote: `github.com/infixrghy/infixr-website` (public)
- live: `infixrghy.github.io/infixr-website/` (Pages source = GitHub Actions)

## §V INVARIANTS
V1: total `src/js/*.js` byte size ≤ 5120 B (non-recursive glob — top-level only). EXEMPT: `src/js/hero-3d/*` (model-viewer runtime + lazy loader, T27 Ari-approved budget exception). hero-3d ships ONLY via lazy injection (desktop + motion-allowed), 0 bytes on the critical path; the static webp is the LCP element and the only hero pixel for reduced-motion / mobile / no-JS.
V2: ∀ new `<script>` tag → Ari approval recorded in §T or §B
V3: ⊥ CSS framework import / link
V4: ∀ section → renders & legible @ 360 px viewport width
V5: ∀ animation → opt out via `@media (prefers-reduced-motion: reduce)` OR justify in comment as intentional
V6: hero `<img>` ! `fetchpriority="high"` + explicit `width`/`height` to prevent CLS
V7: ∀ image → `width` + `height` attrs OR `aspect-ratio` CSS to reserve box pre-load
V8: ∀ page → canonical, og:type/url/title/description/image, twitter:card meta present
V9: `@layer reset/tokens/layout/components` order preserved; new rules join existing layer
V10: ∀ user input (form field) → `:user-invalid` styling + HTML5 validation gate before fetch
V11: form fetch failure → human fallback msg w/ `mailto:contact@infixr.com`
V12: ⊥ hardcoded px breakpoint when `clamp()` or container query works
V13: GH Pages serves `public/` (built artifact) via GitHub Actions. `public/index.html` = home. `.nojekyll` emitted into `public/`.
V14: ∀ hero-class viewport unit → `svh` not `dvh`/`vh` (avoid mobile URL-bar resize jank)
V15: ∀ raster image ≥ 100 KB → `<picture>` w/ webp `<source>` first, original as fallback `<img>`
V16: source = `src/` (HTML, `src/css/*.css`, `src/js/*`, `src/assets/`). `build.ts` reads `src/` → emits `public/`. ⊥ build writes back to `src/`. ⊥ hand-edit `public/` (generated). CI rebuilds on push; local edits auto-fire build via `.claude/settings.json` PostToolUse hook.
V17: ⊥ external `<link rel="stylesheet">` in any built HTML. ⊥ cross-origin font fetch (rsms.me, fonts.googleapis.com, etc). all CSS inlined, all fonts self-hosted from `src/assets/`.
V18: tokens.css ! encode Figma palette: --c-bg:#222526; --c-fg:#e0e0e0; --c-accent:#1FD1A1
V19: body font = Satoshi, self-hosted from `assets/Satoshi-Variable.woff2` (fontshare.com source), w/ system-ui fallback chain. ⊥ Inter. ⊥ Google Fonts.
V20: type scale uses `clamp()`. heading 4.5em–6em; section 3em–4em; subheading 1.5em–2em; body 1em–1.125em. rem-based.
V21: spacing scale = 8 px multiples. tokens --s-1 (8px) … --s-20 (160px). section pad = clamp(120px,12vw,160px). heading→para gap = 24px. para→cta gap = clamp(32px,4vw,40px)
V22: ∀ button → 3 variants (primary filled, secondary outline, tertiary link+arrow). ! ≥ 44×44 px tap target. hover state non-color (transform OR border). reduced-motion safe.
V23: nav top-right links: WHO WE ARE · OUR SOLUTIONS · PRODUCTS · BLOGS · CONTACT US (button). collapse to hamburger ≤ 640 px viewport.
V24: large card → rounded 16–24 px, dark bg, product img top, h2 + body + CTA. hover state defined. carousel uses `scroll-snap`. reduced-motion → autoplay stops.
V25: `build.ts` emits cache-busted asset hashes (e.g. `hero.abc1234.webp`) in HTML refs. ⊥ stale browser cache on deploy.
V26: ∀ token in `src/css/tokens.css` ! referenced ≥ 1 time in `src/css/{components,pages,layout,reset}.css`. dead token → `bun run lint` warns.
V27: hero → sticky header. headline "VR Solutions, Built Around Your <cycler>." in Satoshi; cycler word = accent (--c-accent) gradient, rest = --c-fg. word-cycler KEPT — mechanism = per-word FALL: 5 `<li>` grid-stacked in one cell (inline-grid sizes slot to widest word; ⊥ position:absolute → 0-width collapse), each runs shared `word-fall` keyframe staggered by animation-delay (10s loop, 2s each, visible spans OVERLAP so ⊥ blank frame). word drops in from ~0.9em above, holds, drops out bottom. li `line-height ≥ 1.4` REQUIRED: `background-clip:text` paints gradient only inside line box → tight line-height clips g/y/p/j descenders (B5). slot `height 1.5em` + `overflow:hidden` masks fall. reduced-motion → `word-fade` (swap, ⊥ translate; the fall is NEW motion past the grandfathered cycle). CTAs = "Request a Demo" (primary) + "Explore Our Work" (ghost), UPPERCASE dark pill shape. nav links UPPERCASE incl active HOME. bg = FLAT --c-bg (⊥ full-bleed photo); decorative headset floats RIGHT + CSS/SVG orbital ring = SINGLE static perspective-flattened ellipse + 3 static waypoint nodes (1 teal accent w/ glow). ring ! animated: headset (model-viewer auto-rotate) is the ONE point of motion in the hero — a still frame around moving focal content beats competing rotations. ⊥ restore ring-spin/node-pulse (removed deliberately, ! a regression). text ! WCAG AA contrast. @360px: headset hidden (text-priority, AA-safe). hero 3D SHIPPED (T27): static webp is the LCP element + the hero pixel for mobile/reduced-motion/no-JS; `src/js/hero-3d/loader.js` lazily upgrades it to an auto-rotating self-hosted model-viewer (`headset.glb`) ONLY on desktop (≥900px) w/ motion allowed, on idle, over the webp. built-in neutral lighting → ⊥ cross-origin HDR (V17 holds). model-viewer runtime EXEMPT from V1 budget (see V1). SEAMLESS HANDOFF (B6): the static webp IS the model's exact frame 0 — captured FROM the live model-viewer renderer (`mv.toBlob`) frozen at `camera-orbit 0deg 82deg 105%` (headset front/lenses facing the visitor, slight top-down for dimension; loader sets the SAME orbit so first-paint matches), transparent bg, NOT an external/Blender render (only model-viewer's own renderer pixel-matches its lighting/AO/FOV). `.hero__headset` + `.hero__model` share an IDENTICAL square box (`clamp(360px,38vw,600px)`, `aspect-ratio:1/1`) so footprints align (boxDelta=0). reveal swap is INSTANT (⊥ opacity fade — pixels identical, fade only risks a double-image). auto-rotate is NOT an init attr: loader sets it in the `load` handler AFTER reveal (next rAF) so rotation begins FROM frame 0, ⊥ ~tens-of-degrees ahead. ⊥ CSS drop-shadow on the still (capture bakes in model-viewer's own contact shadow; a 2nd shadow desyncs the swap).
V28: §who-we-are carousel → CENTER card = light/white, dark text LEFT + white product-headset photo RIGHT (`assets/who-headset-white.{webp,png}`, cropped from the PNG mock), sitting FLAT (mock center card is flat). card figure ! definite-height container (`display:grid`) so `object-fit:cover` crops the wide shot to the cell — ⊥ intrinsic-aspect collapse (B4). FLANKING peek panels = gradient-LIT headset PHOTO (`assets/who-headset-flank.{webp,png}`, pink→blue lit product shot cropped from the PNG — NOT a CSS gradient; the headset silhouette shows through the colored light, which conic/linear-gradient can't fake, per B3 PNG-supersedes). set as `background-image` on fixed `::before/::after` edge panels over the viewport (`::after` mirrored via `scaleX(-1)`), width 19%, dark→`--bg` overlay fades the photo inward. environmental not per-card (CSS can't select the mid-autoplay centered slide — still true). peek geometry: 74% slides + 3% gap (0 new JS). default + hover state, indicator dots (active = wide teal pill), clickable nav via CSS radio-label (pins+centers a slide, halts autoplay). autoplay keyframe reduce-gated. section heading CENTERED: eyebrow "WHO WE ARE" + "VR For Real World Challenges" + "Learn More →". @360px: card stacks (photo over text); edge panels frame it. ⊥ new JS > 5 KB.
V29: §our-solutions = ASYMMETRIC grid: 2 small dark-glass cards (Corporate Training, Workforce Training — text only) + 1 large card (Industrial & Safety Training) with industrial/warehouse photo bg + overlaid text, spanning 2 rows. glassmorphism on dark cards (`backdrop-filter: blur()` + semi-transparent bg + border highlight), progressive-enhance via `@supports`. EXACTLY 3 cards (mock's empty filler cards ⊥ shipped). heading "FROM IDEA TO IMMERSIVE REALITY" (uppercase) LEFT + descriptor right + "VIEW MORE →". @360px: 1-col stack.
V30: §blogs → IMAGE-FORWARD overlay cards: title text overlaid on photo (white over scrim, AA by construction — reuse hero scrim pattern). 2 large feature cards (headset photo bg) top row + mixed grid below: small cards (gradient-headset bg, CSS gradient — no asset) w/ date + read-time meta + large people-in-VR photo card. heading "THE FUTURE OF SPATIAL EXPERIENCES" (uppercase). @360px: 1-col. KEEP real Phase-A copy (⊥ lorem from mock).
V31: `public/` is build output only — gitignored, ∄ in commits. deploy = CI builds fresh from `src/`. ∀ asset referenced by built HTML ! exist under `src/assets/` (build copies to `public/assets/`).
V32: ∀ `src/css/*.css` → native CSS nesting. child/variant/state rules nested in parent block (`&:hover`, `& .child`, nested `@media`/`@supports`). ⊥ flat repeated selector chains where nesting works (`.a{} .a .b{} .a:hover{}` → nest under `.a`). ⊥ preprocessor — browser-native nesting only. `@layer` blocks stay; rules nest inside them. exception: cross-cutting base rules legitimately shared by unrelated selectors may stay flat.
V33: build = Effect.ts SSG. shared chrome (head/nav/footer) rendered ONCE from `src/templates/*.ts` as typed `Data → string` fns — ⊥ templating DSL, ⊥ 3 hand-copied chrome blocks (the drift that gave missing-Home-link / stale-hardcoded-year / mismatched-footer-label is structurally ∄ now). page meta Schema-validated w/ `Option` (⊥ null) → malformed config fails BUILD not browser. page bodies = `src/pages/*.body.html` partials; blog body data-driven from `content/posts/*.md`. ACCEPTANCE = byte-diff identity: `base=""` path-prefix threads thru renderHead/renderNav/renderFooter/assembleShell and `base=""` ! be string-IDENTITY → root pages (index/about/blog) byte-stable vs pre-refactor `.baseline`; ⊥ any `../` prefix leakage on root output. Effect/marked = build-time libs, 0 client bytes (✓ V3/V17).
V34: ∀ `content/posts/<slug>.md` → ! emit standalone `public/blog/<slug>.html` (blog-index cards link there; pre-refactor those were dead links — marked `bodyHtml` computed then discarded). post page uses `base="../"` so root-relative refs (favicon, manifest, fonts, `js/form.js`, sibling pages, `index.html#…` anchors) resolve one dir deep (now `js/main.js`, was `form.js`); canonical + og:url carry FULL `https://infixr.com/blog/<slug>.html`. post body wrapped in `.prose` which ! style every tag marked emits (h2/p/ul/ol/li/strong/a) — ⊥ fall to browser defaults. cites V30 (blog), V33 (templated chrome), V32 (`.prose` native-nested).
V36: GLASS CARD unified. the frosted-glass surface on §our-solutions text cards (V29) AND §blogs homepage teaser text cards (V30/V35) = ONE recipe `.glass-card` in `src/css/components.css` (was 2 near-identical `@supports` blocks; deduped). parameterised by 5 CSS custom props — `--gc-tint` (accent mixed INTO surface), `--gc-alpha` (surface opacity), `--gc-rim` (lit border colour), `--gc-ring`/`--gc-halo` (inner/outer accent rings) — defaulting to variant **V3** ("Refined": tint 10% / alpha 50% / rim accent60%, the Ari-chosen look). presets `.glass-card--{v1,v2,v3}` reset the dials (v3 = no-op alias). resting: `blur(22px) saturate(170%)` + 1.5px lit teal rim + tinted translucent bg. hover: lift -8px, `blur(28px) saturate(185%)`, rim brightens, + a diagonal SHEEN streak (`::after`, 1.05s sweep L→R, masked by `.u-card` overflow:clip, card body z-raised so text stays crisp). CONTRAST PRINCIPLE: the card's teal lives ON THE CARD (tint+rim), ⊥ by dimming the section glow — the frost REFRACTS `.blog::before`/`.solutions::before` (the glow IS the card's light source), so muting the glow dims the card interior and DROPS contrast (learned empirically). progressive-enhanced: glassy fill inside `@supports (backdrop-filter)`, base opaque `.u-card` = fallback (V13-style). reduced-motion: sheen off + lift off, static frost+rim kept (V5). 0 client JS — pure CSS effect + build-time component (✓ V1/V3). typed authoring path = `glassCard(params)` (`src/templates/glass-card.ts`) serving BOTH text-card shapes (blog/article AND Solutions) from ONE signature, params Schema-validated (`src/schema/glass-card.ts` — bad variant / out-of-range tint% / malformed footer fails BUILD, cites V33). shape diffs modelled to make invalid states unrepresentable: `href`→Option (Some=linked title, None=plain h3), `eyebrow`→Option, `footer`→Option<Union<{_tag:meta,date,readMinutes}|{_tag:cta,label,href}>> (a CHOICE ⊥ two flags → "both meta&cta" + "half-pair" impossible; template switches `_tag` exhaustively, missing case = tsc error ⊥ silent empty footer). meta footer OWNS the `<time datetime>` (built from date+readMinutes via displayDate, ⊥ flat-string meta that'd drop the machine-readable date). bare `.glass-card` class = low-level escape hatch for shapes the component ⊥ model; the overlay PHOTO cards (`.blog-feature`/`.solution-feature`) stay bespoke (different shape). FULL SPEC = `design/GLASS-CARD.md`. amends V29 (glassmorphism detail now centralised) + V30 (blog cards were image-overlay; the homepage TEASER text cards are now glass per V35's 3-card hybrid). hero CTA pills also carry an accent-rim treatment (distinct from glass-card; `.hero__cta` in components.css) — same teal vocabulary, ⊥ same recipe.

V37: ∀ page (incl FUTURE pages) → cross-document View Transitions opt-in present. mechanism = `@view-transition { navigation: auto }` + `::view-transition-group(root)` duration .35s ease, in `src/css/layout.css` TOP-LEVEL (outside @layer — at-rule registration inside a cascade layer unreliable) wrapped in `@media (prefers-reduced-motion: no-preference)`. NAMES NOTHING deliberately: default `root` snapshot clips to VIEWPORT → opacity-only crossfade, ⊥ transform → sticky header + footer hold still FOR FREE (nothing translates), only `<main>` content crossfades. ⊥ name `main`/`.site-header`/`.site-footer`: a named element snapshots its full border-box → home's tall `<main>` vs about's short `<main>` would tween the HEIGHT delta = vertical SQUASH (the opposite of a crossfade). UNIVERSAL BY CONSTRUCTION: rule lives in shared inlined CSS (`build.ts` → every page's `<style>`), ⊥ per-page opt-in → new pages inherit it automatically (cites V16/V33 shared-chrome — this IS how "even new pages" is guaranteed: ∄ a place to forget it). progressive-enhance: Firefox (2026) + older browsers navigate instantly, ⊥ breakage. reduced-motion → instant nav (V5). 0 client JS (✓ V1/V3). VERIFY ONLY by real cross-doc nav in Chrome/Edge 126+ — fails SILENTLY (a non-firing transition is pixel-identical to normal nav, ⊥ console error). cites V5, V9, V16, V33.

V38: the "dead grey" Ari disliked = `color-mix(in oklab, …, white)` on the cool base `--c-bg #222526` desaturates → flat grey. FIXED in THREE places, each by the means that fits (NOT one global token — a `--lift` teal-tinted-white experiment was tried + REVERTED: at dark surface luminances a tint diluted thru a white-based lift is imperceptible, ~+0.007 green-lead, below the ~+0.04 visibility floor — it changed nothing visible, so it's dead complexity):
  (a) CONTACT FIELDS — active-field focus bg mixes `var(--accent)` DIRECTLY (`color-mix(in oklab, var(--c-bg) 84%, var(--accent))` ≈16%) = a deliberately VISIBLE teal well (browser-measured green-lead +0.018 grey → +0.076 teal), matching the teal ring + border + new `caret-color: var(--accent)`. a diluted/white-based lift here gives ~1.8% effective accent = invisible → focus MUST use the direct mix.
  (b) BLOG + ABOUT CARDS — were `.u-card .u-card--text` w/ the grey `--bg-card`/`--bg-card-hover` surface. Ari wanted "the same glass effect the home page cards have" → add `.glass-card .glass-card--v3` (V36) to their class lists (blog `renderFeatured`+`renderCard` in `src/blog.ts` stay `<article>`; about 4 `<li>` in `src/about.ts`) — class-add, ⊥ swap to `glassCard()` component (component emits `<li>` + only `<h3>`; blog needs `<article>` + featured `<h2>`). `.glass-card` is element-agnostic, drops onto any `.u-card`, + its own hover overrides the grey `.u-card--text:hover{--bg-card-hover}`. CRITICAL CO-REQUISITE: glass frost REFRACTS light behind it → backdrop-filter frosts NOTHING on a flat dark section (V36 + [[infixr-home-blog-glass]]: glow+frost ship together). both pages were flat → ADD a section glow: `.blog-list` gets `position:relative; isolation:isolate` + `::before` accent bloom (top-weighted: behind featured + grid head); `.value-grid` (nested mid-`.prose`, ⊥ its own section) gets the same on the GRID itself (centered bloom, `inset` negative to spread past the cards). blog grid = 1-col (only homepage `.blog .blog__grid` is the 3-col mosaic) so a center-line glow covers every card; about grid = 2-col under the 760px `.prose` measure → centered bloom covers all 4. browser-verified both: edge/outer cards refract (⊥ flat panes).
  (c) FOOTER SOCIAL — `.social-links a` resting disc was `background: var(--bg-card)` (same grey) → now `color-mix(in oklab, var(--accent) 12%, transparent)` faint-teal fill + accent-28% hairline + accent-tinted icon; hover still fills solid `--accent`. part of the teal vocabulary at rest, ⊥ grey.
  PLUS contact-card dim-on-hover bug (V10/V36): `.contact__card:hover` (a `.u-card`) ! restate `box-shadow` too — base `.u-card:hover` sets transform+border+box-shadow ALL at (0,2,0); restating only transform+border let the rich AT-REST frosted shadow (0,1,0) LOSE → card DIMMED on hover (accent rims vanished, glow tightened). fix = `@supports &:hover` restates the at-rest shadow w/ glow nudged UP (`0 0 70px -8px var(--accent-glow)`) → hover WARMS ⊥ dims (the "glow a little on hover" Ari asked). GENERAL: a child suppressing an inherited hover ! override EVERY prop the equal-specificity base hover sets, ⊥ only the noticed one (2nd specificity trap in this file — see [[infixr-contact-form-glass]] for the 1st). raw palette `--c-{bg,fg,accent}` + surface tokens `--bg-{elev,card,card-hover}` UNCHANGED (V18 holds — the white-lift reverted). 0 client JS (pure CSS, ✓ V1/V3). reduced-motion: glass sheen+lift off / focus lift off, colour+ring+rim kept (V5). VERIFY = visual side-by-side in Chrome/Edge (glass refraction + green-lead are eyeball/measure calls, ⊥ build-checkable). cites V5, V9, V10, V18, V36, and [[infixr-home-blog-glass]] glow-behind-frost.

V35: home body PROMOTED from static partial (`src/pages/index.body.html`, now ∄) → render fn `renderHomeBody(posts)` in `src/home.ts` (amends V33 "page bodies = `*.body.html` partials": now ONLY about is a partial; index + blog are render fns). TWO reasons: (a) §blogs homepage cards were a hand-copied snapshot of `content/posts/*.md` that DRIFTED — all 5 linked `blog.html` not the per-post page, read-times were invented (featured said "7 min", front-matter said 4), top-5 was frozen so new posts never surfaced. now data-driven: `posts.slice(0,5)` (newest-first, same sort as blog index) into a FIXED 5-cell positional mosaic (`HOME_BLOG_SLOTS`: surface + grid-area + badge per slot = LAYOUT, stays literal; title/date/read-time/href = DATA). ∀ card deep-links `blog/<slug>.html` w/ real `readMinutes` — ⊥ drift vs `content/posts/`. NOTE: home = newest-5 (⊥ `featured` — that's blog-index only; they coincide today); slot↔topic now positional ⊥ art-directed (slot 5 = people photo regardless of post). (b) the repeated webp+png `<picture>` block (hero + 3 who cards + solution feature) → typed `picture()` helper (`src/templates/*.ts`) — ⊥ copied srcset / mismatched w/h (cites V15 `<picture>` rule). ACCEPTANCE = byte-diff identity (per V33): hero/who/solutions/contact byte-stable vs pre-change `index.html` baseline; only deltas = blog deep-links + real read-times (intended) + `<picture>` attr-order/indent (inert) + 3 dropped layout comments. ⊥ new client JS (pure build-time). cites V30, V33, V34, V15.

V39: NEON-GLOW text effect EXTRACTABLE → reusable presentational CLASS, ⊥ TS component. rationale: the TS render fns (`button()`/`picture()`/`glassCard()`, V33/V36) exist to generate MARKUP VARIATION (label/variant/link-union, webp+png pair) — neon generates ∅ markup, it decorates text already on the page → a `neonText()` wrapper emitting `<span class>` is dead weight. right primitive = class in `@layer components`, `.glass-card`/`.u-card` idiom. FIRST instance = "Get In Touch" contact eyebrow (`.contact__card` block, components.css): lit state = brighter-than-accent fill (`color-mix(in oklab, var(--accent) 88%, white)` — glyph ! be the light SOURCE, brighter than its halo, ⊥ a dim sticker under a lamp) + 4-LAYER `text-shadow` neon bloom (0-offset, blur 4/12/26/46px, accent alpha 80/65/50/35% via `color-mix(…,transparent)` — stacked tight-core→wide-ambient halos read as emitted light; ONE blurred shadow = drop-shadow ⊥ neon) + `transition: color/text-shadow .25s`. CONTRAST w/ the CALMER tier = blog-card title hover (`.blog .blog-text`, components.css): `color: var(--accent)` SWAP ONLY (grey→teal, ⊥ shadow, ⊥ brighten) — a HUE change ⊥ a glow. these are TWO deliberate tiers; "make neon reusable" ⊥ silently convert the quiet hovers to neon — keep the distinction. EXTRACTION DECISIONS when built (T33): (1) SEPARATE effect FROM trigger — class defines ONLY the lit state; each call site wires its own (`.u-neon:hover` / `.parent:hover .u-neon` / `:focus-visible` / always-on). ⊥ bake card-hover into the class (the current `&:hover .eyebrow` ancestor trigger is CONTEXTUAL — works only ∵ eyebrow lives in `.contact__card` — ⊥ portable). (2) TOKENIZE the dials (4 blur radii + 4 alphas + lit-fill mix → custom props w/ defaults, the V36 5-dial pattern) so call sites retune intensity/hue ⊥ forking. (3) USE JUDICIOUSLY — `text-shadow` is PAINT-bound (repaints each frame, ⊥ GPU-composited like `transform`/`opacity`) → fine for a few accent moments, perf smell if sprayed site-wide; scope it. clean API reveals itself at use-site #2 → design the prop surface AFTER a 2nd target named (don't speculate the trigger). VERIFY = rendered SCREENSHOT in Chrome, ⊥ computed-style: mid-transition computed `text-shadow` reports the ANIMATING FRAME (multi-layer shadow transitions init each layer to transparent `oklab(0 0 0 / 0)` + interpolate toward target) → a synthetic-hover read can falsely show all-transparent layers while pixels glow; disable transitions (`* {transition:none}`) to collapse computed to the final target, OR trust the screenshot (the hover-state cousin of B6's synthetic-`.focus()` lie). 0 client JS (pure CSS, ✓ V1/V3). reduced-motion: hover-state colour+glow swap w/ short transition is ⊥ looping motion → ⊥ RM-gate required (consistent w/ contact-card hover, V38); a future ALWAYS-ON neon = static, also fine. cites V5, V9, V32, V36, V38.

V40: FIELD-SIZING auto-grow on the contact MESSAGE textarea ONLY (`#cf-message name="message"`, `home.ts:371`, inside `.field--full`). `field-sizing: content` → the box grows w/ typed content, ⊥ fixed-height scroll. SCOPE = that ONE textarea (Ari-explicit): ⊥ a bare `textarea {}` rule (would auto-grow every future textarea by default) — scope to `#cf-message` (or `.field--full textarea`, the only one today). KEEP `min-height:140px` (contact.css:71) as the FLOOR (field-sizing collapses an empty box to ~`rows` height else; the recessed well must stay tall at rest). ADD a `max-block-size` ceiling (≈`50vh`/`clamp`) so a paste-bomb ⊥ grow unbounded past the viewport → inner scroll past the cap. `rows="5"` stays the no-support fallback height. progressive-enhance: wrap in `@supports (field-sizing: content)` — un-supporting engines keep today's `resize:vertical` + `min-height` box (⊥ regression). 0 client JS (replaces the JS-autosize many sites ship → ✓ V1). ⊥ motion → V5 N/A. lands in `src/components/contact/contact.css` `@layer components` (V9/V32). cites V1, V9, V10, V32.
V41: `@property` REGISTERS the neon dials (V39) so they're (a) DEDUPED + (b) animatable + (c) type-guarded. TODAY `--neon-fill` + `--neon-1..4` are declared VERBATIM-TWICE (`typography.css:44-48` AND `u-card.css:75-79`) — DRY smell. CORRECTED MECHANISM (B7 — the obvious "`initial-value` = the V39 mix" is IMPOSSIBLE): a registered prop's `initial-value` MUST be COMPUTATIONALLY-INDEPENDENT — `var()` is FORBIDDEN there (it's baked at registration, before the cascade resolves `--accent`). The V39 mix is `color-mix(in oklab, var(--accent) …)` → putting it in `initial-value` makes the parser REJECT the whole `@property` rule → dials unregistered → neon silently breaks, AND build.ts (concat, ⊥ CSS-validate) ships GREEN (B7 lying channel). So SPLIT registration from value: (1) `@property --neon-fill {syntax:"<color>"; inherits:true; initial-value: color-mix(in oklab, #1FD1A1 88%, white)}` ×1 + `--neon-1..4` likewise — `initial-value` = STATIC color (accent INLINED as `#1FD1A1`), a fallback ONLY (never used: `:root` always sets the dials). (2) the LIVE values = ONE `var(--accent)`-based `--neon-*` block hoisted to `:root` in `tokens.css` `@layer tokens` — keeps palette-tracking, ⊥ a `#1FD1A1` fork (V18/V33). then BOTH duplicate component blocks delete → inherit from `:root` (call sites that RETUNE keep their local sets). CRITICAL: the photo-feature card LOCAL OVERRIDE of `--neon-fill` (keeps white fill on `.blog-feature`/`.solution-feature`) lives at `u-card.overrides.css:39` `.u-card--overlay{--neon-fill:#f4f5f6}` (NOT `u-card.css:68` — that line is only the COMMENT; V41 was imprecise) → it ! SURVIVE: an explicit per-selector decl beats BOTH the `:root` value (specificity) AND `initial-value` (which is unset-fallback only); verify in-browser post-dedup. BONUS: registered `<color>` props are INTERPOLABLE → neon could TRANSITION fill/blur (unregistered custom-prop animates discrete) — opt-in, ⊥ required. LAYER PLACEMENT: `@property` registration is LAYER-AGNOSTIC (global wherever it sits), but per V37's "at-rule-in-layer unreliable" caution it goes TOP-LEVEL in `layout.css` BESIDE `@view-transition` (the established top-level at-rule home), ⊥ inside `@layer`; browser-VERIFY it registers (empty computed value = rejected). `@supports` NOT needed (unregistered = un-typed/discrete = today's behaviour) but a guard documents intent. 0 client JS (✓ V1). cites V1, V9, V18, V33, V36, V39.
V42: `@starting-style` entry-anim for the contact form STATUS msg (`.contact-form__status`, contact.css:93) — the ONE genuine target. NAV MENU EXPLICITLY EXCLUDED: `.primary-nav ul` (nav.css:55-73) ALREADY animates in/out (scale+opacity transition, always-rendered ⊥ `display:none`) → `@starting-style` adds ∅ there; ⊥ touch it (an earlier "animate the nav menu" idea is RETRACTED post-audit). status msg TODAY has NO `display:none` empty state (`:not(:empty)` only styles; element always in flow) → to animate APPEARANCE needs a RESTRUCTURE: empty→`display:none`, `:not(:empty)`→`display:block` (or grid) + `transition: opacity .25s, transform .25s` + `transition-behavior: allow-discrete` (so the `display` flip is animatable) + `@starting-style { opacity:0; transform: translateY(...) }` for the enter-from. result = success/error message FADES+slides in when JS injects text, ⊥ a hard pop. progressive-enhance: un-supporting engines skip the enter anim, msg still shows (⊥ blocked). reduced-motion: `@media (prefers-reduced-motion: reduce)` → drop the transform (keep instant show) per V5. JS UNCHANGED (still just sets `.textContent` + `data-state`; the CSS does the rest → ✓ V1 ⊥ new JS). lands in contact.css `@layer components` (V9/V32). cites V5, V9, V10, V32.
V43: `:has()` parent-state — when a contact field's input is invalid, the field's LABEL reacts too (today only the input itself reddens, V10). `.field:has(:user-invalid) label { color: <error-tint> }` (+ optional `:has(:user-valid)` accent on the label). extends V10 (input `:user-invalid` styling) UP to the sibling label via the parent. `:has()` is baseline (all major engines since late 2023) → ⊥ `@supports` guard needed (un-supporting = label stays default, ⊥ broken). SCOPE: the `.field` form group ONLY (don't spray `:has()` site-wide). ⊥ re-do the footer-grid auto-fit with `:has()` (the V-less container-query span shipped this session works — ⊥ churn). 0 client JS (✓ V1). lands in contact.css `@layer components` (V9/V32). cites V9, V10, V32.
V44: `text-wrap: pretty` on long-form prose — LARGELY ALREADY SATISFIED, recorded for completeness ⊥ as net work. `reset.css:10` ALREADY sets `p, h1..h6 { text-wrap: pretty }` site-wide; `reset.css:11` upgrades h1–h4 to `balance`; `.prose p` (`u-card.overrides.css:29`) + carousel copy (`carousel.css:195`) also `pretty`. so `.page-lead`/`.blog__lead` (both `<p>`) ALREADY inherit `pretty`. NET-NEW GAP ≈ ∅. ONLY conceivable extension = prose holders the reset's `p`/heading selectors ⊥ reach: `.prose li` (list items) + footer `address` — marginal, OPTIONAL. ⊥ a full task; if touched, a 1-line add of `text-wrap: pretty` to `.prose li` in pages.css. Firefox lacked `pretty` early-2026 → pure progressive-enhance (un-supporting = normal wrapping). cites V9, V20, V34.

V45: ∀ build-injected `<link rel="preload">` ! point at a resource the SAME page's rendered output actually consumes (img/src/srcset/CSS `url()`). a preload nothing uses = ANTI-perf: `fetchpriority=high` steals bandwidth from the real LCP + console "preloaded but not used" is the only signal (build ships GREEN — B6/B7 lying-channel class). root risk: preload targets live in build.ts DECOUPLED from the markup that uses the asset → they outlive redesigns silently (B8: hero2 preload survived V27's flat-bg redesign by ~weeks). conditionally-shown images (hero headset = `display:none` <900px) → preload ! `media=`-gated, else same ghost for the hidden-viewport users. guard = output-based check: ∀ preload href in emitted HTML → same path appears ≥1× elsewhere in that page as a NON-preload use. cites V27, V33.

V46: ∀ file under `src/assets/` ! referenced ≥1× by what ships — assets/ is copied WHOLESALE to public/ (V31), so an unreferenced file deploys forever. dead asset → delete (park future-material renders OUTSIDE src/, e.g. gitignored design/). guard = lint pass over BUILT output (the V26 dead-token idiom, new channel): ∀ `public/assets/<file>` → filename appears ≥1× in `public/**/*.{html,js,webmanifest}` EXCLUDING preload hrefs (a preload is a hint ⊥ a use — counting it lets B8-class ghosts self-justify). src-side grep is INSUFFICIENT: who-/sol- paths are template-CONSTRUCTED (`assets/${s.img}.webp`) → literal filename ∄ in src; og-image referenced by ABSOLUTE URL → filename-match, ⊥ path-match. cites V26, V31.

V47: `<head>` rendered FINISHED. renderHead consumes meta + inline-css + preloads as PARAMS → emits the complete head; ⊥ post-assembly regex mutation of built HTML (marker-replace, link-strip, head-splice). regex post-processing fails SILENT: pattern stops matching → degraded output ships green (B8 hid in exactly this layer). the inlineAndPreload strip-regexes (rsms.me, `css/` stylesheet links, stale preloads) guard markup ∄ render fn can emit — dead since pages became render fns (V33/V35); the `<!--CSS_INLINE-->` marker pair (duplicated head.ts + build.ts) is the same hand-written-HTML-era scar. refactor acceptance = byte-identical `public/` vs pre-refactor baseline (the V33/V35 idiom). cites V33, V35.

## §T TASKS
id|status|task|cites
T1|x|scrape infixr.com content + assets|I.asset
T2|x|scaffold HTML + CSS + JS structure|C
T3|x|hero w/ Effect-style word cycler|V5,V6
T4|x|who-we-are carousel (CSS-only autoadvance)|V5
T5|x|solutions grid|V7
T6|x|blog teaser section|-
T7|x|contact form|V10,V11
T8|x|footer|-
T9|x|mobile audit @ ≤ 360 px|V4
T10|x|inline SVG logo|-
T11|x|about.html|I.page
T12|x|blog.html|I.page
T13|x|Open Graph + favicons|V8
T14|x|GH Pages deploy|V13
T15|x|fix mobile jumpiness near hero+carousel|B1,V6,V7
T16|.|wire real form endpoint (placeholder now)|I.script
T17|.|custom domain config|-
T18|x|fetch Satoshi from fontshare.com → `assets/Satoshi-Variable.woff2`|V19
T19|x|encode Figma tokens in `css/tokens.css` (palette, font, type-scale clamp, spacing-8)|V18,V19,V20,V21
T20|x|build 3 button variants + nav + card components per Figma|V22,V23,V24
T21|.|extend `build.ts`: inject Satoshi @font-face + `<link rel=preload>` + cache-bust asset hashes|V19,V25
T22|x|add `bun run lint` script: dead-token check `tokens.css` vs `{components,pages,layout}.css`|V26
T23|x|hero: flat bg + headset still right + CSS orbital ring + uppercase nav/CTA + teal cycler|V27
T24|x|who-we-are: peek carousel (light center + gradient flanks) + straight cards + dots + CSS nav|V28
T25|x|our-solutions: asymmetric glass grid (2 dark + 1 photo span)|V29
T26|x|blogs: image-forward overlay cards (2 feature + mixed grid)|V30
T27|x|integrate rotating-metaquest3 hero 3D: self-host model-viewer 4.2.0 + glb, lazy-load over static webp (desktop+motion only), neutral lighting (no x-origin HDR), V1 budget carve-out for `js/hero-3d/*`. Ari-approved JS-budget exception|V17,V27,V1
T28|x|re-skin home to match design/figma-final/png (PNG = ground truth, supersedes PDF)|V27,V28,V29,V30,V4,B3
T29|x|fresh redesign: unify card system (.u-card everywhere, carousel exempt) + kill section voids + native CSS nesting sweep|V32,V24,V29,V30,V4,V9,B4
T30|x|home → render fn (`src/home.ts`): blog cards data-driven from posts (fix drift — deep-links + real read-times + newest-5) + `picture()` helper for repeated webp+png blocks. delete `index.body.html`. byte-stable static sections|V35,V30,V33,V34,V15
T31|x|cross-document view transitions: root crossfade .35s, NAME NOTHING (viewport-clip = opacity-only, header/footer hold), prefers-reduced-motion gated, universal via shared inlined CSS|V37,V5,V16,V33
T32|x|kill the dead-grey where it shows: (a) contact field-focus bg mixes accent DIRECT (visible teal well, +0.018→+0.076) + teal caret; (b) blog+about cards → `.glass-card` (same frost as home) + section glows behind them (frost needs light to refract); (c) footer social discs faint-teal not grey; PLUS fix contact-card dim-on-hover (restate box-shadow → warms not dims). `--lift` white-lift experiment reverted (imperceptible). browser-verified all in Chrome|V38,V36,V10,V5
T33|x|extract neon-glow → reusable `@layer components` class (working `.u-neon`/`.neon-text`): lit-state ONLY (brightened fill + 4-layer text-shadow + transition), trigger left to call site; tokenize the dials (blur×4 + alpha×4 + fill-mix → custom props w/ defaults); migrate the "Get In Touch" eyebrow onto it. BLOCKED on a 2nd use-site (design prop surface AFTER target #2 named) + confirm neon = one tier among hovers ⊥ new default (keep the calm blog-title color-swap tier). use judiciously (text-shadow paint-bound)|V39,V36,V32,V5
T34|x|field-sizing:content on `#cf-message` ONLY (Ari-scoped): `@supports`-gated, KEEP min-height:140px floor + ADD max-block-size ceiling, `rows=5` = fallback. scope to the one textarea ⊥ bare `textarea{}`. browser-verify grow-with-content + cap holds + un-supporting fallback|V40,V1,V9,V10,V32
T35|x|@property register `--neon-fill` + `--neon-1..4` (initial-value = V39 default mix) → DEDUP the verbatim-twice blocks (typography.css:44-48 + u-card.css:75-79) to ONE registration. VERIFY photo-feature `--neon-fill` white override survives (initial-value = unset-fallback only). DECIDE layer placement (top-level vs `@layer` — V37 logs at-rule-in-layer unreliable; TEST). optional: enable interpolated neon transition. browser-verify neon UNCHANGED on every card + the contact eyebrow|V41,V36,V39,V9,V1
T36|x|@starting-style enter-anim for `.contact-form__status` ONLY (nav menu EXCLUDED — already animates). restructure: empty→display:none, :not(:empty)→display:block + transition-behavior:allow-discrete + `@starting-style{opacity0+translateY}`. RM-gate the transform (V5). JS unchanged (sets textContent+data-state). browser-verify fade-in on submit success/error + reduced-motion instant + un-supporting still shows|V42,V5,V10,V9,V32
T37|x|:has() label-reacts-to-invalid: `.field:has(:user-invalid) label{error-tint}` (+ optional :user-valid accent), extends V10 up to the sibling label. scope to `.field` ⊥ site-wide. no `@supports` (baseline). ⊥ churn the footer-grid span (works). browser-verify label reddens on invalid input, recovers on valid|V43,V10,V9,V32
T38|x|text-wrap:pretty — VERIFY already-satisfied (reset.css:10 covers all p + .prose/.page-lead/.blog__lead inherit). OPTIONAL 1-liner: add to `.prose li` (+ footer address) — the only holders reset's selectors miss. NET work ≈ ∅; close after confirming coverage|V44,V20,V34,V9
T39|x|kill ghost hero preload: DROP heroPreload from build.ts (hero2-1600/900.webp — ∅ references hero2 since V27 flat-bg). real LCP (hero-headset-1200.webp) already eager+fetchpriority=high in markup; re-add a preload ONLY if LCP measurably regresses, then `media="(min-width:900px)"`-gated (headset hidden below 900px — ungated = same ghost for mobile). verify: ∅ "preloaded but not used" console warning + Lighthouse LCP ≤ baseline|V45,B8
T40|x|dead-asset sweep: delete 9 unreferenced files ≈4.35 MB — hero2.png, hero2-1600.png, hero2-1600.webp, hero2-900.webp, sol-realestate.{png,webp}, sol-industrial.{png,webp} (non-feature), InterVariable.woff2 (park future-material renders in gitignored design/ instead, Ari's call which). fix stale §I asset lines (hero2 "hero bg"; sol-{corporate,industrial,realestate} "solutions cards" → truth: sol-corporate = home blog feature, sol-industrial-feature = solutions photo). extend lint.ts w/ the V46 output-based dead-asset pass so orphans ⊥ recur|V46,V31,B9
T41|.|build.ts: render head FINISHED — pass inline-css + preloads into renderHead as params; DELETE inlineAndPreload (5 dead strip-regexes + head-splice regex) + the CSS_INLINE marker dance (constants duplicated head.ts + build.ts). buildPage = decode → render → write. acceptance: byte-identical public/ vs pre-refactor baseline (land AFTER T39 so the baseline already carries the intended preload delta)|V47,V33,V35,B8

## §B BUGS
id|date|cause|fix
B1|2026-05-20|mobile jank @ hero+carousel. causes: hero `<img>` missing w/h (CLS), `92dvh` resized on URL-bar toggle, `backdrop-filter` on sticky header GPU-thrashed during carousel anim|V6,V7,V14
B2|2026-05-20|hero png 1.2 MB → slow LCP on mobile. webp 121 KB / 48 KB now via `<picture>` srcset|V15
B3|2026-05-30|built home diverged from real design. cause: V27-30 distilled from un-renderable PDFs (pdftoppm/IM unavailable) → built to spec-text, not pixels. fix: ffmpeg-crop the PNG mockups (design/figma-final/png) as ground truth, re-skin to match. PNG supersedes PDF as design source. NOT a regression — design never matched pixels|V27,V28,V29,V30
B4|2026-05-30|(a) hero CTAs touched viewport bottom @ scroll-top. cause: `.hero` align-items:center inside min-height:clamp(560,90svh,880) → centered content row sank, CTAs kissed fold. fix: align-items:start + min-height clamp(540,82svh,820) + `.hero__visual` align-self:center (keep headset optically centered). (b) carousel center-card figure showed white band below the wide who-headset-white shot (900×317). cause: `.card figure` not a definite-height container → img fell to intrinsic aspect (165px), object-fit:cover had nothing to crop. fix: `.card figure{display:grid}` → img stretches to stretched cell, cover crops|V27,V28
B5|2026-05-30|cycler word descender (g/y/p/j) clipped at rest on Training/Walkthroughs. cause: `background-clip:text` paints the gradient ONLY inside the line box; `line-height:1.15` was shorter than Satoshi's glyph descender → tail painted outside box = cut, even w/ slot `overflow` loosened (a 2nd, inner clip). NOT the slot clip — `getBoundingClientRect` measured the line box, not the ink, so it falsely read "fits". fix: li `line-height 1.4` (descender room inside paint box) + slot `height 1.5em`. Also reworked cycler to per-word grid-stack + `word-fall` keyframe (fall-from-above, overlapping spans → no blank frame, reduced-motion → fade)|V27,V5
B6|2026-05-30|capturing model-viewer frame 0 to a static webp (seamless 3D handoff). THREE traps, ~lots of cycles: (1) VERIFICATION CHANNEL LIED — `/__capture` dev endpoint writes `src/assets/` but the dev server serves `public/assets/`, and NO build runs on a binary POST (the auto-build hook fires only on css/ts/body.html/md/build.ts edits). So every "opaque% looks good" probe via the served URL read the STALE OLD photo in `public/`, not the new capture. Decode the bytes you WROTE (the blob, in memory), not a round-trip through the serving layer. (2) BLANK CAPTURES — a backgrounded/hidden automation tab throttles `requestAnimationFrame` → model-viewer's WebGL render loop stalls → canvas blank → `toBlob` returns a fully-transparent image (and `load` never fires either: same throttle). `document.hidden`/`visibilityState` must be visible AT capture. GATE: decode each blob in-memory, assert opaque%>20, NEVER POST a blank. (3) WRONG POSE — `auto-rotate` accumulates theta; removing it then setting `camera-orbit "0deg…"` needs a render tick (2× rAF) before `toBlob` or you capture a mid-spin angle, not true frame 0. ALWAYS visually diff the captured still vs the live model frozen at the same orbit — they were a front-3/4 vs rear-3/4 mismatch until the orbit actually settled. base64 is ALSO redacted in the browser-tool return → can't ferry the image out that way; POST to a localhost dev endpoint instead (revert it before commit)|V27,T27
B7|2026-06-10|`@property` `initial-value` for the neon dials (T35/V41) silently breaks if it carries `var()`. cause: a registered prop's `initial-value` MUST be computationally-independent — `var(--accent)` is forbidden (baked at registration, pre-cascade). V41's "initial-value = the V39 mix" (`color-mix(…var(--accent)…)`) → parser REJECTS the `@property` rule → dials unregistered → neon dead. AND build.ts (concat, ⊥ CSS-validate) ships GREEN → the build is a LYING verify channel (B6-class: green ≠ valid CSS). browser-confirmed: var()-bearing `initial-value` → `getComputedStyle` of the unset prop returns EMPTY (rejected); static `#1FD1A1` mix → resolves. fix: SPLIT — `@property` carries a STATIC `initial-value` (accent inlined, fallback-only) top-level in layout.css; LIVE `var(--accent)` values in `:root` (tokens.css). VERIFY @property in-browser (empty computed = rejected), ⊥ trust build-green|V41
B8|2026-06-10|index preloaded `hero2-1600/900.webp` `fetchpriority=high` — a GHOST: ∅ markup/CSS references hero2 (V27's flat-bg redesign killed the hero photo; the build-INJECTED preload outlived it). every homepage visitor fetched 48–121 KB of invisible image at TOP priority, competing w/ the real LCP (hero-headset webp — which had ∅ preload). found by architecture-review grep ⊥ any gate: build green, lint green, the only runtime signal a console warning nothing watched. root cause = preload decoupled from the markup that uses the asset (lives in build.ts regex-splice) + ∅ output-based check|V45
B9|2026-06-10|~4.35 MB dead assets shipped every deploy: assets/ copied WHOLESALE (V31) w/ ∅ reference check → 9 orphans accumulated across redesigns (hero2* from the photo-bg era pre-V27; sol-realestate/sol-industrial non-feature renders from the V29 grid rework; InterVariable.woff2 from pre-Satoshi, V19 says ⊥ Inter). src-side grep can't catch them: who-/sol- refs are template-constructed (`assets/${s.img}.webp`) → the check must run on BUILT output (V46 guard)|V46
