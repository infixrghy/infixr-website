{
  "name": "infixr-website",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "node build.ts",
    "lint": "node lint.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run test/unit",
    "dev": "node build.ts && node server.ts"
  },
  "dependencies": {
    "effect": "4.0.0-beta.78",
    "marked": "^18.0.4"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
