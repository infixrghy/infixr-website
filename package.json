{
  "name": "infixr-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run build.ts",
    "lint": "bun run lint.ts",
    "dev": "bun run build.ts && bun run server.ts"
  }
}
